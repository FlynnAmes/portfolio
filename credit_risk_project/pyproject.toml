[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "credit_risk_project"
version = "0.1.0"
description = "A simple application that predicts default on a loan"
requires-python = ">=3.11"

[tool.pytest.ini_options]
markers = ["unit", "integration"]